package app.search;

/**
 * The type User entry.
 */
public abstract class UserEntry extends SearchEntry {
    /**
     * Initialises the user entry parameters.
     *
     * @param name the name
     */
    public UserEntry(final String name) {
        super(name);
    }
}
